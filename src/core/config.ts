import { IConfig } from "@clarity-types/core";

export let config: IConfig = {
    /* Core */
    longtask: 50,
    /* Interactions */
    lookahead: 250,
    distance: 20,
    /* Data */
    delay: 1000,
    upload: null
};
