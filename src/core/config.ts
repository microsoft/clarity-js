import { IConfig } from "@clarity-types/core";

let config: IConfig = {
    pageId: null,
    userId: null,
    projectId: null,
    longtask: 30,
    lookahead: 500,
    distance: 20,
    delay: 1000,
    showText: false,
    cssRules: false,
    tokens: [],
    url: "",
    upload: null
};

export default config;
