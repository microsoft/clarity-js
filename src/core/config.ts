import { IConfig } from "@clarity-types/core";

let config: IConfig = {
    longtask: 50,
    lookahead: 250,
    distance: 20,
    delay: 250,
    showText: false,
    cssRules: false,
    tokens: [],
    upload: null
};

export default config;
